import 'package:flutter/material.dart';

class NewPostTab extends StatelessWidget {
  const NewPostTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('NewPostTab Tab'));
  }
}
